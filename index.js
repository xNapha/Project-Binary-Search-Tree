const Node = (a,b = null,c =null) =>{
    const value = a;
    const left = b;
    const right = c;
    return {value, left, right}
}

const Tree = (arr) =>{
    const root = arr;

    const buildTree = (arr) =>{
        
    }

    return {buildTree}
}